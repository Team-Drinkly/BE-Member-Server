server:
  port: 0

spring:
  config:
    import: optional:configserver:http://localhost:8888
  application:
    name: member-service
  profiles:
    active: local

  datasource:
    url: jdbc:mysql://127.0.0.1:3306/drinkly?serverTimezone=Asia/Seoul&characterEncoding=utf8
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update